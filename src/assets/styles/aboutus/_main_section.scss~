@use "../variables" as *;
@use "../mixins/flex" as *;
@use "../mixins/grid" as *;

.aboutus-main-section-container {
  padding-block: calc($XlSize4 - 4px) 292px;
  position: relative;

  &::after {
    content: "";
    position: absolute;
    inset: 24px;
    background-image: url('~/public/images/bg-testimonial-dark.png');
    background-size: auto 100%;
    background-repeat: no-repeat;
    z-index: -1;
    background-position: bottom;
  }

  .container {
    @include flex-row(nowrap, center, center);
    gap: calc($XlSize4 + 112px);
    @media (max-width: 768px) {
      flex-direction: column;
      // padding: $XsSize2;
      gap: $XlSize3;
    }

    

    .main-section-container {
      @include flex-column(nowrap, start, start);
      gap: calc($XlSize3 - 2px);
      width: 50%;

      @media (max-width: 768px) {
        width: 90%;
        margin: auto;
      }

      .main-section-subtitle {
        @include flex-row(nowrap, flex-start, center);
        gap: $XsSize2;
        background-color: $YellowColor;
        padding: calc($XsSize + 1px);
        border-radius: $XlSize4;

        p {
          color: $TextColor8;
          font-size: $SmSize;
          font-weight: $BaseFontSemiBoldWeight;
        }

      }

      .main-section-text {
        @include flex-column(nowrap, center, flex-start);
        gap: $XsSize;

        .main-section-title {
          font-size: $XlSize4;
          font-weight: $BaseFontWeightBold;
          color: black;
          width: 100%;
        }

        .main-section-description {
          color: $TextGray3;
          font-size: $XlSize;
          font-weight: $BaseFontMediumWeight;

        }
      }


      &.main-section-container-icon {
        gap: calc($XlSize + 1px);
        @include flex-column(nowrap, center, center);

        .main-section-icon {
          margin: auto;
        }

        .main-section-description {
          font-size: calc($XlSize2 + 2px);
          font-weight: $BaseFontSemiBoldWeight;
          color: #3D4C5E;
        }
      }
    }
  }
}
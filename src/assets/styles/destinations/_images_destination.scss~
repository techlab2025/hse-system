@use "../variables" as *;
@use "../mixins/flex" as *;


.images-destinations {

  margin-top: -2rem;
  margin-bottom: 2rem;
  padding-inline: $XlSize2;
  @include flex(row, nowrap, center, center);
  gap: $XsSize;
  position: relative;
  z-index: 555;

  @media screen and (max-width: 1100px) {
    @include flex-column();
  }

  .right-image, .left-image {
    width: clamp(12rem, 16vw, 17rem);
    height: 30rem;
    object-fit: cover;
    @media screen and (max-width: 1100px) {
      width: 100%;
      height: 15rem;
    }
    //border-radius: $XsSize;
  }

  .image-swipe {
    width: clamp(44rem, 45vw, 50rem);
    //height: 30rem;
    @media screen and (max-width: 1000px) {
      width: 100%;
    }

    swiper-container {
      &::part(bullet) {
        background-color: #fff;
        //width: 8px;
        //height: 45px;
        border-radius: 20px;
      }

      &::part(bullet-active) {
        background-color: #fff;
        //width: 8px;
        //height: 45px;
        border-radius: 20px;
      }
    }
  }
}